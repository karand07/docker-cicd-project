version: "3.9"

services:
  backend:
    build: ./backend
    ports:
      - "5000:3000"  
    env_file:
      - ./backend/.env
    depends_on:
      - db
    networks:
      - backend-network
      - frontend-network
    restart: always
  frontend1:
    build: ./farmer_frontend
    ports:
      - "3000:80"
    depends_on:
      - backend
    networks:
      - frontend-network
    restart: always
  frontend2:
    build: ./company_frontend
    ports:
      - "3001:80"
    depends_on:
      - backend
    networks:
      - frontend-network
    restart: always
  frontend3:
    build: ./admin_frontend
    ports:
      - "3002:80"
    depends_on:
      - backend
    networks:
      - frontend-network
    restart: always

  db:
    image: mongo
    volumes:
      - mongo-data:/data/db
    networks:
      - backend-network   
    restart: always

volumes:
  mongo-data:

networks:
  backend-network:
  frontend-network: